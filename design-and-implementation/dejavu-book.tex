\documentclass[a4paper,extrafontsizes]{memoir}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{backnaur}
\usepackage{makeidx}
\title{Design and Implementation of the Déjà Vu Programming Language}
\author{Robin Wellner}
\input{dejavu-book-style}
\makeindex
\newcommand\dv{Déjà~Vu}
\usepackage{tikz}
\definecolor{specialblue}{rgb}{0.6,0.6,1}
\newcommand{\udensdot}[1]{%
	    \tikz[baseline=(todotted.base)]{
		\node[inner sep=1pt,outer sep=0pt] (todotted) {#1};
		\path (todotted.base west) ++(1pt, -2pt) coordinate (Start);
		\path (todotted.base east) ++(-1pt, -2pt) coordinate (End);
		\draw[dotted,specialblue,line width = 0.5mm] (Start) -- (End);
	}%
}%

\newcommand\defini[1]{\index{#1}\udensdot{#1}}
%\newcommand\sig[2]{$(-#1\!+\!#2)$}
\newcommand\sig[2]{\raisebox{0.2ex}{\scriptsize(\!$-$#1+#2)}}
\begin{document}

\include{titlepage}

\frontmatter

\include{foreword}

\tableofcontents

\include{introduction}

\mainmatter

\include{source}

\include{bytecode}

\include{compiler}

\include{virtual-machine}

\include{standard-library}

\backmatter

\printindex

\end{document}
